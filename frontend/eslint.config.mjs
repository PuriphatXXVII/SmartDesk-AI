// Flat ESLint config (ESLint 9 + Next.js 16).
// `next lint` was removed in Next 16, so we run ESLint directly via the
// eslint-config-next flat preset (Next core-web-vitals + TypeScript rules).
import next from "eslint-config-next";

export default next;
