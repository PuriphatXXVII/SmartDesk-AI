# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
.next/
out/
dist/
.turbo/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
.DS_Store

# Logs
*.log
npm-debug.log*

# DB / data
*.sqlite
*.db
postgres_data/
redis_data/

# Uploaded documents (local storage; S3/R2 in prod)
backend/uploads/

# Local widget demo page contains a dev widget_key — keep it out of git.
# (frontend/public/smartdesk.js IS committed so the deployed site can serve the
#  embeddable widget at <origin>/smartdesk.js — rebuild + commit it on changes.)
frontend/public/widget-demo.html
widget/dist/

# Misc
.coverage
htmlcov/

# Local-only project notes + editor tooling (kept on disk, not published)
.claude/
CLAUDE.md
